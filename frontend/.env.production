# Production — loaded when NODE_ENV=production (`npm run build`, `npm start`)
# Use http:// until nginx + SSL serve WordPress on :443; then switch to https://
NEXT_PUBLIC_WORDPRESS_URL=http://64.23.192.136
